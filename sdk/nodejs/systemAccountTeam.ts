// *** WARNING: this file was generated by the Pulumi Terraform Bridge (tfgen) Tool. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * SystemAccountTeam Resource
 */
export class SystemAccountTeam extends pulumi.CustomResource {
    /**
     * Get an existing SystemAccountTeam resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: SystemAccountTeamState, opts?: pulumi.CustomResourceOptions): SystemAccountTeam {
        return new SystemAccountTeam(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'konnect:index/systemAccountTeam:SystemAccountTeam';

    /**
     * Returns true if the given object is an instance of SystemAccountTeam.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is SystemAccountTeam {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === SystemAccountTeam.__pulumiType;
    }

    /**
     * ID of the system account. Requires replacement if changed.
     */
    public readonly accountId!: pulumi.Output<string | undefined>;
    /**
     * ID of the team. Requires replacement if changed.
     */
    public readonly teamId!: pulumi.Output<string>;

    /**
     * Create a SystemAccountTeam resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: SystemAccountTeamArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: SystemAccountTeamArgs | SystemAccountTeamState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as SystemAccountTeamState | undefined;
            resourceInputs["accountId"] = state ? state.accountId : undefined;
            resourceInputs["teamId"] = state ? state.teamId : undefined;
        } else {
            const args = argsOrState as SystemAccountTeamArgs | undefined;
            if ((!args || args.teamId === undefined) && !opts.urn) {
                throw new Error("Missing required property 'teamId'");
            }
            resourceInputs["accountId"] = args ? args.accountId : undefined;
            resourceInputs["teamId"] = args ? args.teamId : undefined;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(SystemAccountTeam.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering SystemAccountTeam resources.
 */
export interface SystemAccountTeamState {
    /**
     * ID of the system account. Requires replacement if changed.
     */
    accountId?: pulumi.Input<string>;
    /**
     * ID of the team. Requires replacement if changed.
     */
    teamId?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a SystemAccountTeam resource.
 */
export interface SystemAccountTeamArgs {
    /**
     * ID of the system account. Requires replacement if changed.
     */
    accountId?: pulumi.Input<string>;
    /**
     * ID of the team. Requires replacement if changed.
     */
    teamId: pulumi.Input<string>;
}
